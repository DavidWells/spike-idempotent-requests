{
  "name": "spike-idempotent-requests",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "test": "npm run test:backend",
    "test:backend": "cross-env IDEMPOTENCY_TABLE=test-idempotency-table pnpm exec vitest run tests/unit tests/integration",
    "deploy": "sls deploy --stage prod"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.11.0",
  "devDependencies": {
    "cross-env": "^7.0.3",
    "osls": "^3.51.0",
    "vitest": "^3.1.3"
  },
  "dependencies": {
    "@aws-lambda-powertools/idempotency": "^2.19.1",
    "@aws-lambda-powertools/logger": "^2.19.1",
    "@aws-sdk/client-dynamodb": "^3.808.0",
    "@aws-sdk/lib-dynamodb": "^3.808.0"
  }
}
